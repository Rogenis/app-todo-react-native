import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listEmptyTitle: {
    color: '#fff',
    fontSize: 14,
    textAlign: 'center'
  },
  listEmptySubtitle: {
    color: '#808080',
    fontSize: 14,
    textAlign: 'center'
  }
})